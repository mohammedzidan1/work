import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

import 'componant.dart';

class BisAndTrisPage extends StatelessWidget {
  const BisAndTrisPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CustomScreen(
      centerTitle: "Bi's and Tri's",
    );
  }
}
